# 网络数据包捕获与分析器 - 功能验证步骤

## 项目概述

本项目是一个基于 WPF 和 SharpPcap 的网络数据包捕获与分析器，支持实时捕获、协议解析、过滤、统计、TCP流重组、HTTP分析和ARP欺骗检测等功能。

---

## 前置条件

1. **操作系统**：Windows 10/11
2. **.NET SDK**：.NET 9.0 或更高版本
3. **管理员权限**：捕获网络数据包需要管理员权限
4. **WinPcap 或 Npcap**：确保已安装 WinPcap 或 Npcap（SharpPcap 依赖）

---

## 验证步骤

### 1. 环境准备验证

**步骤**：
1. 打开命令提示符或 PowerShell
2. 运行命令：`dotnet --version`
3. 确认输出显示 .NET 9.0 或更高版本

**预期结果**：
```
9.0.101
```

---

### 2. 编译验证

**步骤**：
1. 打开命令提示符，导航到项目目录
2. 运行命令：`dotnet build`
3. 观察输出结果

**预期结果**：
- 编译成功，无错误
- 可能有关于 OpenTK 和 SkiaSharp 的兼容性警告（可忽略）
- 输出中显示 "成功生成"

---

### 3. 启动应用程序

**步骤**：
1. 以**管理员身份**运行命令提示符
2. 导航到项目目录
3. 运行命令：`dotnet run`
   - 或运行编译后的可执行文件：`bin\Debug\net9.0-windows\NetworkPacketAnalyzer.exe`

**预期结果**：
- 应用程序窗口正常打开
- 窗口标题："Network Packet Analyzer"
- 界面包含：菜单栏、工具栏、过滤栏、数据包列表、详情面板、十六进制视图、状态栏

---

### 4. 网络接口列表验证

**步骤**：
1. 查看工具栏中的网络接口下拉列表
2. 点击下拉箭头查看可用的网络接口
3. 或点击工具栏上的 "Refresh" 按钮刷新接口列表

**预期结果**：
- 下拉列表中显示系统上的所有网络接口
- 每个接口显示描述信息（如 "Microsoft"、"Realtek" 等）

---

### 5. 实时数据包捕获验证

**步骤**：
1. 从下拉列表中选择一个活动的网络接口
2. 点击工具栏上的 "Start" 按钮（绿色播放图标）
3. 观察数据包列表
4. 等待几秒钟，让数据包累积
5. 点击 "Stop" 按钮停止捕获

**预期结果**：
- 数据包列表中实时显示捕获到的数据包
- 每行显示：序号、时间、源IP、目的IP、协议、长度、信息
- 状态栏显示捕获状态和数据包总数
- 协议列以不同颜色显示（TCP-蓝色、UDP-绿色、HTTP-紫色等）

---

### 6. 数据包详情验证

**步骤**：
1. 开始捕获一些数据包
2. 在数据包列表中点击选择任意一个数据包
3. 观察下方左侧的详情树状视图
4. 观察下方右侧的十六进制视图

**预期结果**：
- 详情面板显示数据包各层的解析信息：
  - **Ethernet II**：源MAC、目的MAC、类型
  - **IPv4**：版本、头部长度、总长度、TTL、协议、源IP、目的IP
  - **TCP/UDP**：源端口、目的端口、序列号、标志位等
  - **HTTP**（如果有）：方法、URL、Host、头部等
- 十六进制视图显示原始数据包的十六进制和ASCII表示
- 选中详情树中的节点时，十六进制视图应显示对应的数据

---

### 7. 过滤器功能验证

**步骤**：
1. 确保已捕获一些数据包
2. 在过滤框中输入过滤表达式（见下方示例）
3. 按 Enter 键或点击 "Apply" 按钮
4. 观察数据包列表的变化
5. 点击 "Clear" 按钮清除过滤器

**常用过滤表达式示例**：

| 表达式 | 说明 |
|--------|------|
| `ip.src==192.168.1.1` | 源IP为192.168.1.1的数据包 |
| `ip.dst==8.8.8.8` | 目的IP为8.8.8.8的数据包 |
| `ip.addr==192.168.1.1` | 源或目的IP为192.168.1.1的数据包 |
| `protocol==TCP` | 仅显示TCP数据包 |
| `protocol==UDP` | 仅显示UDP数据包 |
| `protocol==HTTP` | 仅显示HTTP数据包 |
| `tcp.port==80` | 源或目的端口为80的TCP包 |
| `tcp.srcport==443` | 源端口为443的TCP包 |
| `udp.port==53` | 端口为53的UDP包（DNS） |
| `frame.len>100` | 长度大于100字节的数据包 |
| `http.host contains google` | HTTP Host包含google的数据包 |
| `ip.src==192.168.1.1 && protocol==TCP` | 组合条件：源IP且协议为TCP |

**预期结果**：
- 应用过滤器后，列表中只显示匹配的数据包
- 状态栏显示 "Filtered: X" 表示过滤后的数量
- 清除过滤器后，显示所有数据包

---

### 8. 协议统计饼图验证

**步骤**：
1. 捕获一定数量的数据包（建议至少几十个）
2. 点击工具栏上的 "Statistics" 按钮
3. 观察右侧出现的统计面板
4. 查看协议分布饼图

**预期结果**：
- 右侧显示统计面板
- 饼图显示各协议的分布比例
- 饼图下方有图例，标注每种协议的名称和颜色
- 鼠标悬停在扇区上显示详细信息

---

### 9. 包速率折线图验证

**步骤**：
1. 开始持续捕获数据包
2. 打开统计面板（如果未打开）
3. 观察 "Packet Rate" 折线图
4. 持续观察至少10秒

**预期结果**：
- 折线图实时更新，显示每秒数据包数量
- X轴显示时间（秒）
- Y轴显示每秒包数
- 图表随时间动态刷新（保留最近60秒数据）

---

### 10. PCAP文件保存验证

**步骤**：
1. 捕获一些数据包后停止捕获
2. 点击工具栏上的 "Save" 按钮，或菜单 File → Save As
3. 选择保存位置和文件名（如 test.pcap）
4. 点击保存
5. 查看保存的文件大小

**预期结果**：
- 文件成功保存
- 文件大小大于0
- 状态栏显示保存成功的消息

---

### 11. PCAP文件离线分析验证

**步骤**：
1. 点击工具栏上的 "Clear" 按钮清空当前数据包
2. 点击 "Open" 按钮，或菜单 File → Open
3. 选择之前保存的 .pcap 文件
4. 点击打开
5. 观察数据包列表和统计信息

**预期结果**：
- 成功加载文件中的所有数据包
- 数据包列表显示文件中的所有包
- 协议统计和其他分析功能对离线文件同样有效
- 状态栏显示加载的数据包数量

---

### 12. TCP流重组验证

**步骤**：
1. 捕获包含HTTP或其他TCP流量的数据包
2. 点击工具栏上的 "Streams" 按钮
3. 观察右侧TCP流列表面板
4. 查看流的数量和信息

**预期结果**：
- 列表面板显示所有检测到的TCP流
- 每条流显示：源IP:端口 ↔ 目的IP:端口
- 流的数量与捕获的TCP连接数相符
- 可以看到HTTP请求和响应信息

---

### 13. HTTP URL提取验证

**步骤**：
1. 确保在捕获时有HTTP流量（可以在浏览器中访问一个HTTP网站）
2. 在过滤器中输入 `protocol==HTTP` 过滤HTTP包
3. 选择一个HTTP请求数据包
4. 在详情面板中展开 "HTTP" 节点

**预期结果**：
- 可以看到HTTP请求的类型（GET/POST等）
- 可以看到请求的URL路径
- 可以看到Host头部
- 可以看到完整的URL（http://host/path）
- 可以看到其他HTTP头部信息

**提示**：
- 可以访问 `http://example.com` 等HTTP网站来产生HTTP流量
- 大多数现代网站使用HTTPS，需要访问HTTP网站才能看到明文HTTP

---

### 14. ARP欺骗检测验证

**步骤**：
1. 点击工具栏上的 "Alerts" 按钮打开ARP警报警告面板
2. 在正常网络环境下开始捕获数据包
3. 观察警报告警面板

**验证方法（需要模拟攻击环境）**：
1. 准备两台设备在同一局域网
2. 在一台设备上使用ARP欺骗工具（如arpspoof、cain & abel等）
3. 开始欺骗后观察分析器的警报告警面板

**预期结果**：
- 正常网络环境下：无警报告警
- 存在ARP欺骗时：
  - 警报告警面板显示红色警告
  - 显示被欺骗的IP地址
  - 显示该IP对应的多个MAC地址
  - 状态栏显示告警信息
  - 可以看到告警发生的时间和次数

**注意**：ARP欺骗检测需要在有ARP攻击的环境中才能验证。正常情况下应无告警。

---

### 15. 大量数据包性能验证

**步骤**：
1. 开始捕获数据包
2. 让捕获持续几分钟，或打开一个大的pcap文件
3. 观察界面响应速度
4. 滚动数据包列表，检查是否卡顿
5. 切换选中的数据包，检查详情更新速度

**预期结果**：
- 界面保持响应，无明显卡顿
- 数据包列表滚动流畅
- 切换选中数据包时，详情和十六进制视图及时更新
- 支持数千个数据包的流畅显示

---

## 故障排除

### 问题：无法看到网络接口
**解决方案**：
- 确保已安装 WinPcap 或 Npcap
- 以管理员身份运行程序
- 重新安装 WinPcap/Npcap 后重启

### 问题：捕获不到数据包
**解决方案**：
- 确认选择了正确的网络接口
- 确认网络接口有流量通过
- 检查防火墙设置
- 以管理员身份运行

### 问题：程序崩溃
**解决方案**：
- 确认安装了正确版本的 .NET 运行时
- 查看 Windows 事件查看器中的错误日志
- 尝试在干净环境中重新编译运行

### 问题：图表不显示
**解决方案**：
- 确保捕获了足够的数据包
- 确认统计面板已打开（Statistics按钮）
- 等待几秒钟让数据累积

---

## 项目文件结构

```
shark2/
├── Models/                          # 数据模型
│   └── PacketInfo.cs               # 数据包信息模型
├── Services/                        # 业务服务
│   ├── Parsers/
│   │   └── PacketParser.cs         # 协议解析器
│   ├── Capture/
│   │   └── PacketCaptureService.cs # 数据包捕获服务
│   ├── Filtering/
│   │   └── PacketFilterService.cs  # 过滤引擎
│   ├── PcapFile/
│   │   └── PcapFileService.cs      # PCAP文件读写
│   ├── Statistics/
│   │   └── PacketStatisticsService.cs # 统计服务
│   ├── TcpStream/
│   │   └── TcpStreamReassembler.cs # TCP流重组
│   └── ArpSpoofing/
│       └── ArpSpoofingDetector.cs  # ARP欺骗检测
├── ViewModels/                      # MVVM视图模型
│   └── MainViewModel.cs            # 主视图模型
├── Views/                           # 视图（暂未使用独立文件）
├── Helpers/                         # 辅助工具
├── App.xaml / App.xaml.cs          # 应用入口
├── MainWindow.xaml / .cs           # 主窗口
└── NetworkPacketAnalyzer.csproj    # 项目文件
```

---

## 支持的协议

- **数据链路层**：Ethernet II
- **网络层**：IPv4, ARP
- **传输层**：TCP, UDP, ICMP
- **应用层**：HTTP, DNS (识别)

## 已知限制

1. 仅支持以太网接口的数据包捕获
2. IPv6 支持有限（仅识别，不解析详细字段）
3. TCP流重组为基础版本，不处理乱序和重传
4. HTTP解析仅支持明文HTTP，HTTPS流量无法解密
5. 图表在大量数据时可能影响性能

---

## 技术栈

- **UI框架**：WPF (.NET 9.0)
- **架构模式**：MVVM (CommunityToolkit.Mvvm)
- **数据包捕获**：SharpPcap + PacketDotNet
- **图表库**：LiveChartsCore (SkiaSharp)
